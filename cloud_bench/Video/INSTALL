Video Stream Server Benchmark: ffserver + openRTSP

1. Install the ffserver:
	sudo apt-get install ffmpeg

2. Configure ffserver in /etc/ffserver.conf

3. Start ffserver:
	ffserver -f /etc/ffserver.conf

4. Install the openRTSP:
	sudo apt-get install livemedia-utils

5. Play the video:
	openRTSP rtsp://server_address:7654/test2.mp3
